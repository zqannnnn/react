```jsx
<Filter
    initOptions={{
            type: 'all',
            page: 1,
            pageSize: 6
        }}
    onOptionsChange={(newOptions) =>
        console.log(newOptions) }
            />
```
